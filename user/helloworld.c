#include "kernel/types.h"
#include "user/user.h"

int main(int argc, char *argv[]) {
    // Print the required message to the screen
    printf("Hello World xv6\n");
    
    // Exit the program safely
    exit(0);
}